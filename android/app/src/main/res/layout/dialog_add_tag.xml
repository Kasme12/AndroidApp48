<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tag_type"
        android:textSize="14sp"
        android:layout_marginBottom="4dp" />

    <!-- Spinner (dropdown) for tag type: Person or Location -->
    <Spinner
        android:id="@+id/spinner_tag_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:minHeight="48dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tag_value"
        android:textSize="14sp"
        android:layout_marginBottom="4dp" />

    <!-- AutoCompleteTextView for tag value with suggestions -->
    <AutoCompleteTextView
        android:id="@+id/actv_tag_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/enter_value"
        android:inputType="text"
        android:minHeight="48dp" />

</LinearLayout>
